import React from 'react';
import { Card, Badge } from '../../components/common';

const Projects = () => (
  <div>
    <h1>Projects</h1>
    <p style={{ color: 'var(--text-muted)', marginBottom: '2rem' }}>Manage your portfolio</p>
    <Card>
      <Badge variant="purple">Coming Soon</Badge>
    </Card>
  </div>
);

export default Projects;
